#delimit ;
clear ;
set more 1;
set linesize 200;
*************************************************************;
* Replication program for Chapter 3 in SW4E;
* Results for Table 3.1;
*;
*************************************************************;
log using ch3_4e_gender_box,replace;
***********************************;
* Read In Data; 
* (Note: Change path name so that it is appropriate for your computer);
use "/Users/mwatson/Dropbox/TB/4E/WebSupplements/ForStudents/SW_4E_ReplicationFiles/Replication_Data/cps_ch3.dta";
*;
gen male = (a_sex == 1);
gen female = (a_sex == 2);

preserve;
keep if year==1996;
ttest ahe15, by(female) unequal unpaired;
restore;
preserve;
keep if year==2008;
ttest ahe15, by(female) unequal unpaired;
restore;
preserve;
keep if year==2015;
ttest ahe15, by(female) unequal unpaired;
log close;
clear;
exit;
